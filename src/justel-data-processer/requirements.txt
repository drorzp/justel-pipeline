# JUSTEL Belgian Legal Documents Processing Pipeline Dependencies

# Data processing
pandas>=1.5.0
openpyxl>=3.0.0       # For Excel file support

# Web scraping
httpx>=0.25.0         # Fastest HTTP client with async support
selectolax>=0.3.17    # Fastest HTML parser (C-based)
requests>=2.28.0      # HTTP library
beautifulsoup4>=4.11.0 # HTML parsing

# AWS S3
boto3>=1.26.0         # AWS SDK for Python

# OpenAI integration
openai>=1.0.0         # OpenAI API client

# Text processing
markdown>=3.4.0       # Markdown processing
markdownify>=0.11.0   # HTML to Markdown conversion

# Environment management
python-dotenv>=1.0.0  # Load environment variables from .env file

# Standard library extensions (no additional installs needed)
# asyncio - Built into Python 3.7+
# re - Built into Python
# logging - Built into Python
